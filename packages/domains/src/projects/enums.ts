export enum ProjectStatus {
    ACTIVE = 'active',
    ARCHIVED = 'archived',
    ON_HOLD = 'on_hold'
}

export enum ProjectMemberRole {
    ADMIN = 'admin',
    MEMBER = 'member',
    VIEWER = 'viewer'
}